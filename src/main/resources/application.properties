spring.application.name=backend
# ===============================
# = Data Source Configuration =
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/wheels_hub
spring.datasource.username=root
spring.datasource.password=siddssqlroot@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# =========================
# = JPA / Hibernate Config =
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# =====================
# = JWT Configuration =
# =====================
# IMPORTANT: For production, generate a strong, unique base64 encoded key.
# Example: In Node.js, `require('crypto').randomBytes(64).toString('base64')`
app.jwtSecret=YOUR_SECURE_BASE64_ENCODED_JWT_SECRET_HERE_AT_LEAST_64_BYTES_LONG_FOR_HS512!
app.jwtExpirationMs=86400000
# 1 day in milliseconds
# =====================
# = Server Settings =
# =====================
server.port=8080
# =====================
# = CORS (Optional) =
# =====================
# spring.web.cors.allowed-origins=http://localhost:3000
